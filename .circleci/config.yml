version: 2.1
orbs:
  node: circleci/node@5.0.2
  aws-cli: circleci/aws-cli@3.1.1
  eb: circleci/aws-elastic-beanstalk@1.0.0
jobs:
  build:
    docker:
      - image: 'cimg/base:stable'
    steps:
      - checkout
      - node/install:
          install-yarn: true
          node-version: '16.13'
      - aws-cli/setup:
          profile-name: cli-user
      - eb/setup
      - run: node --version
      - run: yarn --version
      - run: aws --version
      - run: eb --version
      - run:
          name: 'Install Frontend'
          command: cd frontend && yarn install
      - run:
          name: 'Build Frontend'
          command: cd frontend && yarn build
      - run:
          name: 'Test Frontend'
          command: cd frontend && yarn test
      - run:
          name: 'Deploy frontend'
          command: cd frontend && yarn deploy

      # setup Elastic Beanstalk
      - run:
          name: 'Setup Elastic Beanstalk environment variables'
          command: |
            eb setenv AWS_ACCESS_KEY_ID=$AWS_ACCESS_KEY_ID AWS_DEFAULT_REGION=$AWS_DEFAULT_REGION AWS_SECRET_ACCESS_KEY=$AWS_SECRET_ACCESS_KEY BCRYPT_PASSWORD=$BCRYPT_PASSWORD TOKEN_SECRET=$TOKEN_SECRET SALT_ROUNDS=$SALT_ROUNDS ENV=$ENV RDS_HOSTNAME=$RDS_HOSTNAME RDS_PORT=$RDS_PORT RDS_DB_NAME=$RDS_DB_NAME RDS_USERNAME=$RDS_USERNAME RDS_PASSWORD=$RDS_PASSWORD


      - run:
          name: 'Install Backend'
          command: cd backend && yarn install
      - run:
          name: 'Build Backend'
          command: cd backend && yarn build
      - run:
          name: 'Test Backend'
          command: cd backend && yarn test
      - run:
          name: 'Deploy backend'
          command: cd backend && yarn deploy


workflows:
  storefront:
    jobs:
      - build:
          filters:
            branches:
              only:
                - main

